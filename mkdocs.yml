site_name: '🌱: `xmod`: Turn an object into a module 🌱'

theme:
  name: material

  palette:
    primary:
      'lightBlue'

  font:
    text:
      Roboto
    code:
      Roboto Mono

extra_css: [extra.css]

plugins:
    - mkdocstrings
